`{{ amount }} {{ currencySymbol }}` {{ description }}
[Link](<{{ transactionLink }}>) | [Select Category](<{{ serviceUrl }}/transaction/{{ transactionId }}/categories?api_token={{ apiToken }}>)
{% for category in categories %}
- [`{{ category.attributes.name }}`](<{{ serviceUrl }}/transaction/{{ transactionId }}/category/{{ category.id }}?api_token={{ apiToken }}>)
{% endfor %}
