`{{ transaction.amount | toFixed(transaction.currency_decimal_places) }} {{ transaction.currency_symbol }}` {{ transaction.description }}

[Transaction](<{{ transactionId | UrlFireflyTransactionShow }}>)

[Select Category](<{{ transactionId | UrlSparkleftCategorySetUI }}>)

[New rule](<{{ transaction | UrlFireflyNewRule }}>)

[Rules](<{{ "" | UrlFireflyRules }}>)

{# {% for category in categories %} #}
{# - [`{{ category.attributes.name }}`](<{{ transactionId | UrlSparkleftCategorySet(category.id) }}>)   #}
{# {% endfor %} #}

|  |  |  |
|--|--|--|
{% for group in categoriesGroups %}
|{% for category in group %} [{{ category.attributes.name }}](<{{ transactionId | UrlSparkleftCategorySet(category.id) }}>) | {% endfor %}{% for _ in range(3 - group.length) %} |{% endfor %}

{% endfor %}
